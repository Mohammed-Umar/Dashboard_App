import { Component} from '@angular/core';


@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {

  /**
   * 1. Create a Item details page.
   * 2. Add filter to Every list page.
   * 3. Show list only after filter selection.
   * 4. Remove Additional info from list page.
   * 5. Remove App_Object_ID & Additional_Info fields from ADD NEW and UPDATE page.
   * 6. Add a close button in ADD NEW & UPDATE page.
   * 7. Add a soft close delete button in list page beside update button.
   * 8. In ADD NEW & UPADATE pages drop-down's should have name's insted of ID's.
   * 9. Add Admin user field in LIST, ADD NEW, UPDATE & DETAILS pages.
   * 10. Look for any client side QR code generator.
   * 11. Show the QR code in the bottom of DETAILS page.
   * 12. In DETAILS page add a button for printing the QR code.
   * 13. Add a checkbox "isCritical" for equipments in ALL pages.
   * 14. QR code functionality is more specific to equipments.
   */

}
